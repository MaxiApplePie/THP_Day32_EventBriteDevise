<div class="container">

<section class="jumbotron py-3 text-center container">
<div class="row py-lg-3">
  <div class="col-lg-6 col-md-8 mx-auto">
    <h1 class="fw-light">Rejoignez nous</h1>
    <p class="lead text-muted">Pour sortir, rencontrer les personnes qui habitent à coté de chez vous, c'est ici. <br /><small>Spécial, week-end chez Félix les 3 et 4 juillet, venez nombreux et amenez du bon son</small></p>
    <p>
      <a href="<%=new_event_path%>" class="btn btn-primary my-2">Votre evenenement</a>
      <a href="<%=new_user_registration_path%>" class="btn btn-secondary my-2">Vous inscrire</a>
    </p>
  </div>
</div>
</section>

<div class="album py-3 bg-light">
<div class="container">

<div class="row row-cols-1 row-cols-md-2 g-4">

<% @events.each do |e| %>
<div class="col">
  <div class="card">
    <%if e.img_event.attached?%>
      <%= image_tag e.img_event, alt: 'photo' %>
    <% else %>
      <img src="https://images.pexels.com/photos/2608517/pexels-photo-2608517.jpeg?cs=srgb&dl=pexels-matheus-bertelli-2608517.jpg&fm=jpg", alt:'photo' class="img">
    <%end%>
    <div class="card-body">
      <h5 class="card-title"><%= e.title %></h5>
      <p class="card-text"><%= e.description %></p>
      <a href=<%=event_path(e.id)%> class="btn btn-primary">Voir</a>
      <% if (current_user == e.organiser) %>
        <a href="<%= edit_event_path(e.id) %>" class="btn btn-warning">Editer</a>
        <a href="<%= event_path(e.id) %>" class="btn btn-danger" data-confirm="êtes-vous sur?" data-method="DELETE">Supprimer</a>
      <% end %>
 
    </div>
  </div>
</div>
<% end %>

</div>
</div>
</div>
</div>